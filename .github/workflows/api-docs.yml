name: deploy-api-docs
on:
  push:
    branches:
      - main
      - vapor4
permissions:
  contents: read
  id-token: write

jobs:
  build-and-deploy:
    uses: vapor/api-docs/.github/workflows/deploy.yml@1905791f101ea5b9311754b77f1a606a88c6752c # main
    secrets: inherit
    with:
      package: vapor
      ref: ${{ github.ref_name }}
