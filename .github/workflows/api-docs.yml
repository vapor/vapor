name: deploy-api-docs
on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
     uses: vapor/api-docs/.github/workflows/build-and-deploy-docs-workflow.yml@e2cc84996c23078c7998aed372c736f14f14b811
     secrets: inherit
     with:
       package_name: vapor
       modules: Vapor,XCTVapor
