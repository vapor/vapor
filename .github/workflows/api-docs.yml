name: deploy-api-docs
on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
     uses: vapor/api-docs/.github/workflows/build-and-deploy-docs-workflow.yml@56c6cc0e4cb8a2295f1d9ff178daefdcba3c2937
     secrets: inherit
     with:
       package_name: vapor
       modules: Vapor,XCTVapor
