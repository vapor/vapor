let currentlyTesting = false
/*import Spectre

public class Tests {
	public class func run() {
		describe("a redirect") { test in
			let redirect = Redirect(to: "http://google.com")

			test.it("has an address") {
				try expect(redirect.redirectLocation) == "http://google.com"
			}

			test.it("is a 301") {
				try expect(redirect.status.code) == 301
			}

			test.it("has 'Location' header") {
				let headers = redirect.headers()
				try expect(headers["Location"]) == "http://google.com"
			}
		}
	}
}*/