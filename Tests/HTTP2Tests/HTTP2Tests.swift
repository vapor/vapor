import XCTest
@testable import HTTP2
import Pufferfish

public class HTTP2Tests: XCTestCase {
    static let allTests = [
        ("testClient", testClient)
    ]
    
    func testClient() throws {
        
    }
}
